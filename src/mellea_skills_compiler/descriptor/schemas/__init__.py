"""JSON Schema resources for the descriptor validator.

This package exists so that :func:`importlib.resources.files` can locate
``descriptor.schema.v*.json`` reliably across editable, wheel, and
egg-info installs.
"""
